# Facility Booking API

A Laravel RESTful API to manage rooms, bookings, and roles.

## Postman Collection
You can find the collection and environments under `/postman`.

## Setup
1. Run `composer install`
2. Configure `.env`
3. Run `php artisan migrate --seed`
